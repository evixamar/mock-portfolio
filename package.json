{
  "private": true,
  "name": "mock-portfolio",
  "homepage": "https://mock-portfolio-evixamar.netlify.com",
  "scripts": {
    "deploy": "netlify deploy --prod --dir=public",
    "postdeploy": "echo 'Your site was deployed to https://mock-portfolio-evixamar.netlify.com'",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "stylelint": "^9.10.1",
    "stylelint-config-standard": "^18.2.0"
  }
}
